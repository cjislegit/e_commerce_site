import styles from './RelatedProducts.module.css';

const RelatedProducts = () => {
  return (
    <section className={styles.related}>
      <div className={styles.related__container}>Test</div>
    </section>
  );
};

export default RelatedProducts;
